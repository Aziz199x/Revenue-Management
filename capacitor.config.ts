import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.fluffyturtletrot',
  appName: 'fluffy-turtle-trot',
  webDir: 'dist'
};

export default config;
